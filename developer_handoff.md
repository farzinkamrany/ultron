# Ultron: Developer Handoff & Codebase Guide

این داکیومنت به زبون ساده بهت یاد میده که ربات از چه بخش‌هایی تشکیل شده، کدهای اصلی کجا هستن، و اگر خواستی تغییری توش بدی دقیقاً باید کدوم فایل رو باز کنی.

---

## ۱. ساختار پوشه‌ها (Directory Structure)

کل پروژه‌ی تو بر پایه **Next.js** نوشته شده و از **TypeScript** استفاده می‌کنه.

```text
ultron/
├── src/
│   ├── app/                 # مسیرهای API (جایی که سرور و کرون‌جاب‌ها اجرا میشن)
│   │   └── api/
│   │       ├── cron/        # کرون‌جاب‌ها (مثل چک کردن تریدها هر ۱۵ دقیقه)
│   │       └── telegram/    # وب‌هوک تلگرام (دریافت و ارسال پیام به ربات تلگرام)
│   ├── lib/                 # کتابخانه‌های اصلی و مغز ربات (ترید، هوش مصنوعی، دیتابیس)
│   │   ├── trading/         # تمام استراتژی‌ها و فرمول‌های ریاضی اینجاست
│   │   ├── cto/             # کدهای مربوط به هوش مصنوعیِ برنامه‌نویس (CTO)
│   │   └── ai.ts            # اتصال به Gemini و prompt های اصلی
├── scripts/                 # اسکریپت‌های تست و بک‌تست (مثل megalodon.ts)
├── data/                    # فایل‌های CSV تاریخچه بازار برای بک‌تست
└── HANDOFF.md               # داکیومنت استراتژی و تاریخچه‌ی سیستم (مفاهیم ترید)
```

---

## ۲. موتور ترید لایو (Live Trading Engine) کجا کار می‌کنه؟

وقتی ربات تو لایو داره ترید می‌کنه، ۳ تا فایل اصلی با هم ارتباط دارن:

### الف) مغز متفکر و سیگنال‌دهی: `src/lib/trading/hunter.ts`
- **وظیفه‌اش چیه؟** این فایل بازار رو اسکن می‌کنه (همون ۱۰ تا ارز)، سطوح گن (Gann) رو محاسبه می‌کنه، کندل‌ها رو می‌خونه، و تصمیم می‌گیره که **بخره یا بفروشه**.
- **اگر خواستی استراتژی رو عوض کنی:** مثلاً خواستی فرمول ولوم یا نقطه ورود رو دستکاری کنی، باید این فایل رو باز کنی.

### ب) اجرای ترید روی صرافی: `src/lib/trading/execute.ts` یا `manage-trades`
- **وظیفه‌اش چیه؟** وقتی `hunter.ts` سیگنال داد، این فایل از طریق API (مثل ccxt یا Hyperliquid) اوردر رو می‌فرسته به صرافی.
- **تغییرات:** اگر خواستی اهرم (Leverage) رو عوض کنی یا صرافی رو تغییر بدی، با این فایل‌ها سر و کار داری.

### ج) قلب تپنده (Cron Job): `src/app/api/cron/trading/route.ts`
- **وظیفه‌اش چیه؟** سرور Vercel این فایل رو به صورت اتوماتیک هر ۱۵ دقیقه یک‌بار (از طریق تنظیمات `vercel.json`) صدا می‌زنه. وقتی صدا زده میشه، این فایل به `hunter.ts` میگه: "برو بازار رو بگرد".

---

## ۳. موتور بک‌تست چطور کار می‌کنه؟

تمام تست‌هایی که می‌گرفتیم (اون ۸.۶ میلیون دلار) تو پوشه‌ی `scripts/` هستن.

### فایل `scripts/megalodon.ts`
- **این چیه؟** این محیط شبیه‌ساز ماست. به جای اینکه به صرافی لایو وصل بشه، دیتاهای فایل‌های `data/*.csv` رو می‌خونه و دقیقاً استراتژی لایو رو روی ۶ سال گذشته شبیه‌سازی می‌کنه.
- **تغییرات بک‌تست:**
  - اگر خواستی هرم‌سازی رو تغییر بدی یا فرمول PnL رو دستکاری کنی، باید بیای تو این فایل و بخش `if (closed)` رو ادیت کنی.
  - اگر خواستی یک اندیکاتور جدید تست کنی، اول اینجا می‌نویسیش، اگر تو بک‌تست جواب داد، بعداً کپیش می‌کنی تو `hunter.ts` برای لایو.

---

## ۴. هوش مصنوعی و تلگرام

### الف) تلگرام: `src/app/api/telegram/route.ts`
- هر پیامی که به ربات تلگرامت می‌دی میاد اینجا. این فایل پیام تو رو می‌خونه و تصمیم می‌گیره باهاش چیکار کنه (مثلاً به هوش مصنوعی پاس بده یا دستور رو مستقیماً اجرا کنه).

### ب) مغز کُدنویس (CTO): `src/lib/cto/`
- این پوشه همون چیزیه که به من (هوش مصنوعی) اجازه میده کدهای تو رو اتوماتیک ادیت کنم. از ابزارهایی مثل `ts-morph` استفاده می‌کنه تا کدها رو بدون خراب کردنِ سینتکس تغییر بده. تو معمولاً نیازی به دست زدن به این پوشه نداری مگر اینکه بخوای به رباتت قدرتِ برنامه‌نویسی جدیدی بدی.

---

## ۵. چطور پروژه‌ رو تغییر بدم و تست کنم؟

اگر فردا من نبودم و خودت خواستی چیزی رو تغییر بدی، این روند رو طی کن:

1. **تغییر رو تو بک‌تست تست کن:**
   - فایل `scripts/megalodon.ts` رو باز کن.
   - تغییرت رو (مثلاً عوض کردن مووینگ ۲۰۰ به ۱۰۰) اعمال کن.
   - تو ترمینال بنویس: `npx tsx scripts/megalodon.ts`
   - نتیجه رو ببین. اگر سودت بیشتر شد، برو مرحله بعد.

2. **تغییر رو ببر تو لایو:**
   - فایل `src/lib/trading/hunter.ts` رو باز کن.
   - همون تغییر (مووینگ ۱۰۰) رو اونجا هم اعمال کن.
   
3. **پوش کردن (Deploy):**
   - کدها رو تو گیت‌هاب Commit و Push کن.
   - ورسل (Vercel) اتوماتیک کد جدید رو می‌گیره و آپدیت می‌کنه.

---

## ۶. مفاهیم کلیدی که نباید یادت بره

- **هرم‌سازی هوشمند (Smart Pyramiding):** وقتی پوزیشن تو سوده و می‌خوای حجم رو دو برابر کنی، همیشه استاپ‌لاس کل پوزیشن رو بیار رو نقطه‌ی ورود اولیه‌ات (Breakeven) تا اگر بازار برگشت، صفر ضرر کنی.
- **تاییدیه حجم (Volume Confirmation):** تو تایم‌فریم‌های پایین مثل ۱۵ دقیقه پر از فیک‌اوته. همیشه از `candle.volume > avgVol * 1.5` استفاده کن تا فقط وقتی بازار واقعاً منفجر شد وارد بشی.
- **متغیر `pyramidPrice`:** قیمت هرم‌سازی با قیمت ورود اولیه فرق داره. اگر این رو تو فرمول سودت اشتباه بزنی، بک‌تست بهت توهم میده (همون باگ ۸ میلیونی قدیمی).

همیشه یادت باشه: **کُدها دروغ نمیگن، ریاضیات هرگز احساساتی نمیشه.**
