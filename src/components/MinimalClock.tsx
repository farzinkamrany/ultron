"use client";import{useEffect,useState}from"react";import{Clock,Calendar}from"lucide-react";export function MinimalClock(){const[mounted,setMounted]=useState(false);const[time,setTime]=useState("");const[shamsiDate,setShamsiDate]=useState("");useEffect(()=>{setMounted(true);const updateTime=()=>{const now=new Date();setTime(now.toLocaleTimeString("en-US",{hour12:false}));setShamsiDate(now.toLocaleDateString("fa-IR",{weekday:"long",year:"numeric",month:"long",day:"numeric"}));};updateTime();const interval=setInterval(updateTime,1000);return()=>clearInterval(interval);},[]);if(!mounted){return(<div className="flex items-center gap-3 text-xs font-mono text-slate-500"><span>--:--:--</span></div>);}return(<div className="flex items-center gap-3 font-mono text-xs"><div className="flex items-center gap-1.5 text-phase1 bg-phase1/5 border border-phase1/10 px-2.5 py-1.5 rounded-lg shadow-[0_0_10px_rgba(0,191,255,0.05)]"><Clock className="w-3.5 h-3.5 animate-pulse"/><span className="font-bold tracking-wider">{time}</span></div><div className="hidden sm:flex items-center gap-1.5 text-slate-400 bg-white/5 border border-white/10 px-2.5 py-1.5 rounded-lg"><Calendar className="w-3.5 h-3.5 text-slate-500"/><span className="text-[11px]">{shamsiDate}</span></div></div>);}