"use client";import{useState,useEffect}from"react";import{Clock}from"lucide-react";export function MinimalClock(){const[time,setTime]=useState<string>("");const[date,setDate]=useState<string>("");useEffect(()=>{const updateTime=()=>{const now=new Date();setTime(now.toLocaleTimeString("fa-IR",{hour12:false,hour:"2-digit",minute:"2-digit",second:"2-digit"}));setDate(now.toLocaleDateString("fa-IR",{year:"numeric",month:"short",day:"numeric"}))};updateTime();const interval=setInterval(updateTime,1000);return()=>clearInterval(interval)},[]);if(!time)return null;return(<div className="flex items-center gap-2.5 px-3.5 py-2 bg-black/40 border border-phase1/30 rounded-xl text-xs font-mono shadow-[0_0_10px_rgba(0,191,255,0.1)]"><Clock className="w-4 h-4 text-phase1 animate-pulse"/><div className="flex items-center gap-2"><span className="text-phase1 font-bold tracking-wider">{time}</span><span className="text-slate-500">|</span><span className="text-slate-400 font-medium">{date}</span></div></div>)}