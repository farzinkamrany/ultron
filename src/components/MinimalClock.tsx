"use client";import{useState,useEffect}from"react";import{Clock,Calendar}from"lucide-react";export function MinimalClock(){const[time,setTime]=useState<Date|null>(null);useEffect(()=>{setTime(new Date());const timer=setInterval(()=>setTime(new Date()),1000);return()=>clearInterval(timer);},[]);if(!time)return<div className="w-full h-16 bg-white/5 backdrop-blur-xl border border-white/10 rounded-2xl animate-pulse"/>;const hh=time.getHours().toString().padStart(2,"0");const mm=time.getMinutes().toString().padStart(2,"0");const ss=time.getSeconds().toString().padStart(2,"0");const faDate=new Intl.DateTimeFormat("fa-IR",{year:"numeric",month:"long",day:"numeric",weekday:"long"}).format(time);const enDate=new Intl.DateTimeFormat("en-US",{month:"short",day:"2-digit",year:"numeric"}).format(time);return(<div className="w-full bg-black/40 backdrop-blur-xl border border-phase1/30 rounded-2xl p-4 shadow-[0_0_20px_rgba(0,191,255,0.1)] flex flex-col sm:flex-row items-center justify-between gap-3 relative overflow-hidden"><div className="absolute -right-8 -top-8 w-24 h-24 bg-phase1/10 rounded-full blur-xl pointer-events-none"/><div className="flex items-center gap-3"><div className="p-2.5 rounded-xl bg-phase1/10 border border-phase1/30 text-phase1 shadow-[0_0_10px_rgba(0,191,255,0.2)]"><Clock className="w-5 h-5 animate-pulse"/></div><div className="flex items-baseline gap-1 font-mono"><span className="text-3xl font-extrabold text-slate-100 tracking-wider">{hh}</span><span className="text-2xl font-bold text-phase1 animate-pulse">:</span><span className="text-3xl font-extrabold text-slate-100 tracking-wider">{mm}</span><span className="text-2xl font-bold text-phase1 animate-pulse">:</span><span className="text-sm font-semibold text-phase1/80 min-w-[20px]">{ss}</span></div></div><div className="flex items-center gap-2 border-t sm:border-t-0 sm:border-l border-white/10 pt-2 sm:pt-0 sm:pl-4 text-xs font-mono text-slate-400 w-full sm:w-auto justify-between sm:justify-end"><Calendar className="w-4 h-4 text-phase2 shrink-0"/><div className="flex flex-col text-right sm:text-right"><span className="text-slate-200 font-medium">{faDate}</span><span className="text-[10px] text-slate-400">{enDate}</span></div></div></div>);}